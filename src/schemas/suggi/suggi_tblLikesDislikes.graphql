type LikesDislikes {
  _id: ID!
  UserId: ID
  Category: String
  CategoryId: Int
  Likes: Boolean
  CreatedOn: String
}

type Query {
  suggi_getLikesDislikesById(id: ID!): LikesDislikes
  suggi_getAllLikesDislikes: [LikesDislikes]
}

